{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build current file",
      "type": "shell",
      "command": "g++ -std=c++20 -g \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}.out\"",
      "args": [],
      "problemMatcher": ["$gcc"],
      "group": "build"
    }
  ]
}
